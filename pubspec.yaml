name: todopractice
description: A new Flutter ToDo application.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Updated http dependency version
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  http: ^1.2.2  # Updated http package version

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/



